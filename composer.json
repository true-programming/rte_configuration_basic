{
	"name": "trueprogramming/rte-configuration-basic",
	"type": "typo3-cms-extension",
	"description": "Adds basic configuration for site packages to your TYPO3 project.",
	"license": ["MIT"],
	"require": {
		"typo3/cms-core": "^10 || ^11"
	},
	"extra": {
		"typo3/cms": {
			"extension-key": "rte_configuration_basic"
		}
	}
}
